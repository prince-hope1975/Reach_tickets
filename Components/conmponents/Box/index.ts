import { Box as DefaultBox } from '@mui/system'
import styled from 'styled-components'
import React from 'react'

const Box = styled.div`
    border: 1.5px solid var(--font);
    width: 100%;
    height: 50px;
    padding: 1rem;
    display: flex;
    align-items: center;
    gap: 1rem;

`
export default Box