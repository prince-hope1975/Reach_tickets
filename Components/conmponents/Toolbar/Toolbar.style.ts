import { Toolbar as DefaultToolbar,} from "@mui/material";
import styled from "styled-components";

const Toolbar = styled.div`
display: flex;
justify-content: space-between;
padding: 0;
/* margin: 0; */
`
export default Toolbar
/* Animated videos for your website */
