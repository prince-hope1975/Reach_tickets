import styled from "styled-components";
import { AppBar as DefaultAppBar } from "@mui/material";

const AppBar = styled.nav`
  padding: 1rem;
  margin: 0 ;
  background: var(--clr-bg);
  z-index: 9999;
`;

export default AppBar;